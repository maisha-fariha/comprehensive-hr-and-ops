/// Severity of an item in the "Needs Attention" list.
enum AlertSeverity { critical, urgent }

/// Semantic status tag shown on a "Today's Overview" stat card (drives both
/// the badge label and its color grouping).
enum StatTag { active, urgent, due, review }
